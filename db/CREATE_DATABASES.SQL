CREATE DATABASE "chitter_manager"

CREATE TABLE user(id SERIAL PRIMARY KEY, email VARCHAR(60), password VARCHAR(100));

CREATE TABLE property(id SERIAL PRIMARY KEY, content VARCHAR(280), timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP, user_id INTEGER REFERENCES users (id));

CREATE DATABASE "chitter_manager_test"

CREATE TABLE users(id SERIAL PRIMARY KEY, email VARCHAR, password VARCHAR(100));

CREATE TABLE property(id SERIAL PRIMARY KEY, content VARCHAR(280), timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP, user_id INTEGER REFERENCES users (id));